{
    "version": 2,
    "buildCommand": "ls -la frontend",
    "outputDirectory": "frontend/dist",
    "functions": {
        "api/index.js": {
            "runtime": "vercel-node@latest"
        }
    },
    "routes": [
        {
            "src": "/api(/(.*))?",
            "dest": "/api/index.js"
        },
        {
            "handle": "filesystem"
        },
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}